# Faker Generate Activity on GitHub Profile

It's stupid.

A repository that containers a github action and a python script that uses `faker` library to generate fake inputs in a file and fake commit message full of buzzwords.
